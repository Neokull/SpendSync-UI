# ----------------------------------------------------------------------
# SISTEMA OPERATIVO (Basura de Mac y Windows)
# ----------------------------------------------------------------------
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# ----------------------------------------------------------------------
# IDEs / EDITORES (Archivos de configuración personal que no se comparten)
# ----------------------------------------------------------------------
# Visual Studio Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace

# JetBrains (PhpStorm, IntelliJ, WebStorm)
.idea/
*.iml
*.iws

# ----------------------------------------------------------------------
# NODE.JS / JAVASCRIPT (Cliente)
# ----------------------------------------------------------------------
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json # Opcional: A veces se quiere subir, a veces no. Por defecto NO lo ignoramos, pero si te da guerra, descomenta.
dist/
build/
coverage/

# ----------------------------------------------------------------------
# PHP / SYMFONY / COMPOSER (Servidor)
# ----------------------------------------------------------------------
/vendor/
composer.lock # Igual que package-lock.json: Lo normal es subirlo, pero si solo practicas, puedes ignorarlo.

# Symfony específico
/var/
/public/bundles/
/public/build/
.env.local
.env.local.php
.env.*.local
/config/secrets/prod/prod.decrypt.private.php

# PHPUnit (Tests)
.phpunit.result.cache
/bin/.phpunit/

# ----------------------------------------------------------------------
# DOCKER (Contenedores)
# ----------------------------------------------------------------------
docker-compose.override.yml  # Configuraciones locales de Docker que no quieres subir

# ----------------------------------------------------------------------
# ARCHIVOS DE ENTORNO Y SECRETOS (¡SEGURIDAD!)
# ----------------------------------------------------------------------
.env
*.env
.env.backup
.env.production

# ----------------------------------------------------------------------
# LOGS Y TEMPORALES
# ----------------------------------------------------------------------
*.log
*.tmp
*.temp
*.swp